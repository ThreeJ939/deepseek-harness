/**
 * @deepseek-ai/dsh-platform — network-storage overlay bundle.
 * The package contributes its `cordis.patch.yml` layer; there is no Cordis plugin body.
 * @module @deepseek-ai/dsh-platform
 */

/** Stable marker that this package is a dsh bundle overlay. */
export const bundle = 'platform' as const
